name: azureng-py-aci
runtime:
  name: python
  options:
    virtualenv: venv
description: Creates Azure Container Instance with a public IP
template:
  config:
    location:
      description: Azure region to deploy resources to
      default: WestUS
