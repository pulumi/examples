name: LambdaWebServer
runtime: dotnet
description: Basic example of an AWS serverless website using F#
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
