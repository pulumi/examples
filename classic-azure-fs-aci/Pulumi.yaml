name: azure-fs-aci
description: Deploys a docker container to Azure Container Instances
runtime: dotnet
template:
  config:
    azure:location:
      description: The Azure location to deploy into
      default: westus
    azure:subscriptionId:
      description: The Azure subscription to deploy into
