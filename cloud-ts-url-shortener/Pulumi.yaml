name: url-shortener
runtime: nodejs
description: URL shortener sample
template:
  description: URL shortener sample
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
