name: azure-cs-aks-managed-identity
runtime: dotnet
description: A minimal Azure Native Pulumi C# program
template:
  config:
    azure-native:location:
      description: The Azure region to deploy into
      default: westus2
