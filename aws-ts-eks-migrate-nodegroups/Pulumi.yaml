name: update-nodegroups
description: Creates an EKS cluster with node groups and a workload, and showcases how add an additional node group to use for workload migration with zero downtime.
runtime: nodejs
