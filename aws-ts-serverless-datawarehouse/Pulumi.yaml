name: aws-ts-serverless-datawarehouse
runtime: nodejs
description: Serverless Analytics on AWS
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
    dev:
      description: Set to "true" to run crons at a faster cadence and enable force-destroy on S3 buckets for development
