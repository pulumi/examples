name: azure-nextgen-cs-appservice
description: Creates Azure Nextgen App Service with SQL Database and Application Insights
runtime: dotnet
template:
  config:
    sqlPassword:
      description: SQL Server password (complex enough to satisfy Azure policy)
      secret: true
