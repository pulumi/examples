name: random
runtime: yaml
template:
  description: Generate secure random passwords to use in deployments
resources:
  randomPassword:
    type: random:RandomPassword
    properties:
      length: 16
      special: true
      overrideSpecial: "_%@"
outputs:
  password: ${randomPassword.result}
