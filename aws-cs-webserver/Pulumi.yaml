name: aws-cs-webserver
description: "Basic C# example of an AWS web server accessible over HTTP."
runtime: dotnet
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
