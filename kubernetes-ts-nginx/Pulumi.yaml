name: k8s-nginx
runtime: nodejs
description: Example of a Kubernetes Stateless Application Deployment, using Nginx
template:
  config:
    replicas:
      description: The number of Nginx replicas to run
      default: 2

