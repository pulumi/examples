name: aws-py-secrets-manager
runtime:
  name: python
  options:
    virtualenv: venv
description: A minimal AWS Python Pulumi program
template:
  description: Create a secret and a secret version in AWS Secrets Manager
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
