name: aws-go-fargate
runtime: go
description: An end-to-end AWS ECS Fargate infrastructure as code project.
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
