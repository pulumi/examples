name: azure-cs-webserver
description: "Basic C# example of an Azure web server accessible over HTTP."
runtime: dotnet
template:
  config:
    azure:location:
      description: The Azure location to deploy into
      default: westus
    azure:subscriptionId:
      description: The Azure subscription to deploy into
