name: azure-go-aks-managed-identity
runtime: go
description: A minimal Azure Native Pulumi GO program
template:
  config:
    azure-native:location:
      description: The Azure location to deploy into
      default: westus2
