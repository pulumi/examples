name: aws-ts-eks
description: EKS cluster example
runtime: nodejs
template:
  description: EKS cluster example
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
