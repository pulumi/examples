[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "3.13.1"
pulumi = "==3.142.0"
pulumi-aws = "==6.64.0"
pyright = "==1.1.390"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
