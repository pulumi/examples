name: aws-ts-cdk
description: Use CDK constructs with Pulumi
runtime: nodejs
template:
  description: Use CDK constructs with Pulumi
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
