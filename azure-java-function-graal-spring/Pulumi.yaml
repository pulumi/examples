name: azure-function-graal-spring
description: Deploys Spring Boot application packaged with GraalVM using Pulumi on Azure from Java definitions
runtime: java
template:
  config:
    azure-native:location:
      description: The Azure location to deploy into
      default: westus
