name: azure-go-appservice-docker
runtime: go
description: Creates Azure App Service and Deploys Docker images to it
template:
  config:
    azure-native:location:
      description: The Azure location to deploy into
      default: westus2
