name: aws-ts-create-role
runtime: nodejs
description: Demonstrate use of AWS AssumeRole Functionality
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
    unprivilegedUsername:
      description: The name of the unprivileged IAM user to create
