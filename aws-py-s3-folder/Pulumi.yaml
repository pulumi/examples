name: aws-py-s3-folder
runtime:
  name: python
  options:
    virtualenv: venv
description: A minimal AWS Python Pulumi program
template:
  description: A static website hosted on Amazon S3
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
