name: azure-cs-aks
runtime: dotnet
description: A C# Pulumi program to provision an Azure Kubernetes Service (AKS) cluster
template:
  config:
    azure-native:location:
      description: The Azure region to deploy into
      default: westus
