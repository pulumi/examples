name: azure-cs-functions
description: Creates Azure Functions on a Linux App Service Plan
template:
  config:
    azure-native:location:
      description: The Azure region to deploy into
      default: westus2
runtime: dotnet
