name: azure-py-aks-managed-identity
runtime: python
description: A minimal Azure Native Pulumi Python program
template:
  config:
    azure-native:location:
      description: The Azure location to deploy into
      default: westus2
